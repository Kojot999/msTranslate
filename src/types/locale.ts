export type Dictionary = {
  components: {
    app: {
      loading: string;
      error: string;
      noSupportedLanguages: string;
    };
    header: {
      title: string;
      github: string;
    };
    footer: {
      companyName: string;
      svgrepo: string;
      libreTranslate: string;
    };
    message: {
      tryAgin: string;
      placeHolder: string;
    };
  };
};
