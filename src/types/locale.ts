export type Dictionary = {
  components: {
    header: {
      title: string;
      github: string;
    };
    footer: {
      companyName: string;
      svgrepo: string;
      libreTranslate: string;
    };
    message: {
      tryAgin: string;
    };
  };
  screen: {
    translator: {
      loading: string;
      error: string;
      noSupportedLanguages: string;
    };
  };
};
