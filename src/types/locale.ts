export type Dictionary = {
  components: {
    header: {
      title: string;
      github: string;
    };
    footer: {
      companyName: string;
      svgrepo: string;
      libreTranslate: string;
    };
  };
};
